#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"
export MEMORY_GUIDANCE_VERSION=v0_5
export MEMORY_GUIDANCE_V05_DYNAMIC_NFE="${MEMORY_GUIDANCE_V05_DYNAMIC_NFE:-0}"
export MEMORY_GUIDANCE_V05_FINE_RATIO="${MEMORY_GUIDANCE_V05_FINE_RATIO:-1.20}"
export MEMORY_GUIDANCE_V05_FINE_SCALE="${MEMORY_GUIDANCE_V05_FINE_SCALE:-0.20}"
export MEMORY_GUIDANCE_V05_FINE_CONFIRM_STEPS="${MEMORY_GUIDANCE_V05_FINE_CONFIRM_STEPS:-2}"
export MEMORY_GUIDANCE_V05_DYNAMIC_GUIDANCE_SCALE="${MEMORY_GUIDANCE_V05_DYNAMIC_GUIDANCE_SCALE:-0.50}"
export SERVER_WAIT_SECONDS="${SERVER_WAIT_SECONDS:-600}"
exec bash "${SCRIPT_DIR}/run_libero10_guidance_only_batch8.sh" "$@"
